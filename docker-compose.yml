version: '3'
services:
  bot:
    build: ./
    restart: always
    environment:
      BOT_TOKEN: #delete this comment and paste the bot token here
  mysql:
    image: "mysql:latest"
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment: #you can change this password if you like
      MYSQL_ROOT_PASSWORD: railerbot
    volumes:
      - lchs-bot-data:/var/lib/mysql
    links:
      - bot
      - phpmyadmin
  phpmyadmin:
    image: "phpmyadmin/phpmyadmin:latest"
    restart: always
    environment:
      PMA_HOST: mysql
    ports:
     - "6980:80"
volumes:
  lchs-bot-data: